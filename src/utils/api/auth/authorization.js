// import axios from 'axios';
// import { BASE_URL } from '../../../../config';
// import { Endpoints, RequestTimeout } from '../../constants';

// export const signUpApi = (data) => {
//     return axios.post(`${BASE_URL}${Endpoints.signup}`, data, RequestTimeout.timeout);
// }

// export const resetPasswordApi = (data) => {
//     return axios.post(`${BASE_URL}${Endpoints.resetPassword}`, data, RequestTimeout.timeout);
// }

// export const setPassword = (data) => {
//     return axios.post(`${BASE_URL}${Endpoints.setPassword}`, data, RequestTimeout.timeout);
// }

// export const signInApi = (data) => {
//     return axios.post(`${BASE_URL}${Endpoints.signIn}`, data, RequestTimeout.timeout);
// }